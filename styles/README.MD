# Стили

Папка создана для стилей. Все стили запускаются через главный файл style.css

Подразделенно на два блока:
- base: Содержатся стили контейнера, а также стили шрифтов
- blocks: Содержатся стили на каждую секцию